export type Quote = { exchange: string; symbol: string; base: string; bid: number; ask: number; bidAskAt: number; quoteCurrency: string; settlementCurrency?: string; contractKind?: string };
export type Config = { enabled: boolean; monitorUrl: string; monitorUsername: string; hasMonitorPassword?: boolean; notionalPerLeg: number; maxOpen: number; maxTotalNotional: number; feeBps: number; slippageBps: number; minNetBps: number; takeProfitBps: number; stopLossBps: number; maxHoldMinutes: number; cooldownSeconds: number };
export type Opportunity = { id: string; pairKey: string; base: string; long: Quote; short: Quote; grossBps: number | null; netBps: number | null; eligible: boolean; reason: string; observedAt: number; warnings?: string[] };
export type Valuation = { at: number | null; stale: boolean; net: number | null; gross?: number; exitFees?: number; reason?: string; quoteTimes?: { long: number | null; short: number | null } };
export type Fill = { price: number; quantity: number; notional: number; notionalUSDT?: number; status: string };
export type Position = { id: string; base: string; long: Quote; short: Quote; openedAt: number; closedAt?: number; quantity: number; longFill: Fill; shortFill: Fill; entryFees: number; feeBps: number; valuation: Valuation; result?: { net: number; gross: number; exitFees: number }; reason?: string; unverifiedConstraints?: string[] };
export type State = { now: number; csrfToken: string; venues: { id: string; state: string; quoteCount: number }[]; fx: { currency: string; state: string; bid?: number; ask?: number; at?: number; error?: string }[]; config: Config; source: { state: string; error: string | null; updatedAt: number | null; checkedAt: number | null; generatedAt?: number | null; receivedAt?: number | null; durationMs?: number | null; quoteCount: number }; catalog: { state: string; count: number; updatedAt: number | null; error: string | null }; totals: { openCount: number; closedCount: number; usedNotional: number; realizedPnl: number; unrealizedPnl: number | null; stalePositions: number }; opportunities: Opportunity[]; positions: Position[]; history: Position[]; events: { at: number; message: string }[] };
