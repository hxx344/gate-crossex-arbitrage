HOST=127.0.0.1
PORT=3200
DATA_DIR=/var/lib/gate-crossex-arbitrage
NODE_ENV=production
# INITIAL_PASSWORD is optional; first startup otherwise prints a generated password.
# The existing saved password wins on upgrades.
# No exchange API credentials are used by this paper-only version.
